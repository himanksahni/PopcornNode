
<form action="/movies/{{movieId}}/reviews/create" method="POST">
    <input type="hidden" name="movieId" value={{movieId}}>
    {{>review-form}}
    <div class="form-group">
        <button type="submit" class="btn btn-primary">Submit the review</button>
    </div>
</form>
