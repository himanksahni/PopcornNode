                <div class="col-sm-4">

                <div class="card" style="width: 18rem;"></div>
                <div class="card-body">
                <a class="card-title card-link" href="/movies/{{this.movieId}}/reviews/{{this._id}}"><h2>{{this.title}}</h2> </a>

                <h5 class="card-subtitle mb-2 text-muted">{{this.movieTitle}}</h5>

                <p class="card-text"> {{ this.description}}</p>

                <a class="btn btn-primary" href="/reviews/{{this._id}}/edit">Edit review</a>

<form method ="POST"  action="/reviews/{{this._id}}/?_method=DELETE">
    <br>
    <button type="submit" class="btn btn-danger">Delete review</button>
</form>
            </div>
</div>